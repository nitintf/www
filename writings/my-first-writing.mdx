---
title: My First Writing
date: 2024-01-15
summary: This is a concise summary of my first writing.
keywords: [blogging, contentlayer, next.js, personal website, web development]
---

# This is my first blog post using Contentlayer!

and to be honest i like i'm doing good i just need to be consistent with the stuff i'm planning

## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

This is great right and we can do this as well{' '}
`var name = 'World';{:js}`

```js
const toastsHeightBefore = React.useMemo(() => {
  return heights.reduce((prev, curr, reducerIndex) => {
    // Calculate offset up until current toast
    if (reducerIndex >= heightIndex) {
      return prev;
    }
    return prev + curr.height;
  }, 0);
}, [heights, heightIndex]);

const offset = React.useMemo(
  () => heightIndex * GAP + toastsHeightBefore,
  [heightIndex, toastsHeightBefore],
);
```

```ts
console.log('hewwo'); // [!code --]
console.log('hello'); // [!code ++]
console.log('goodbye');
```
